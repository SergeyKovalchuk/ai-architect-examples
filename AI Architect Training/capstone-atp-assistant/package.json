{
  "name": "capstone-atp-assistant",
  "version": "1.0.0",
  "description": "Capstone: ATP match-assistant chatbot — agent orchestrating RAG + MCP tools (ATP data via Arquero, weather, news).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "atp-server": "tsx src/mcp/atp-server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "arquero": "^8.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
